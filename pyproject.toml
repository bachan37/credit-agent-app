[project]
name = "credit-agent-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Bachan Smruty", email = "bachan37@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langchain-core",
    "langchain",
    "langchain_community",
    "langchain-openai",
    "fastapi",
    "pydantic",
    "python-dotenv",
    "uvicorn",
    "pydantic-settings"
]

[project.scripts]
credit-agent-app = "credit_agent_app:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
